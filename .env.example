# =============================================================================
# Software Project Analyzer — Environment Configuration
# =============================================================================

# PostgreSQL Database Configuration
POSTGRES_DB=analyzer_db
POSTGRES_USER=analyzer_user
POSTGRES_PASSWORD=analyzer_secret_password
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
DATABASE_URL=postgresql://analyzer_user:analyzer_secret_password@postgres:5432/analyzer_db

# Express Backend Service Configuration
PORT=4000
NODE_ENV=production
JWT_SECRET=super_secret_jwt_key_analyzer_2026_phase27
JWT_EXPIRES_IN=7d
ANALYZER_URL=http://analyzer:8000
PYTHON_ANALYZER_URL=http://analyzer:8000
GITHUB_TOKEN=

# Python Analyzer Service Configuration
MAX_REPO_SIZE_MB=500
ANALYSIS_TIMEOUT_SECONDS=300
WORKSPACE_TMP_DIR=/tmp/software-analyzer-workspaces

# React Frontend Service Configuration
VITE_API_URL=/api
