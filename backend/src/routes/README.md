# Routes

Routes define the HTTP endpoints of the application and map them to their corresponding controller functions. They also wire up route-specific middlewares (e.g., authentication, validation).
