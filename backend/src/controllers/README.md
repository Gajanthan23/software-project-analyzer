# Controllers

Controllers contain the Express request handlers. They extract request parameters, validate inputs, call the appropriate business services, and format the JSON responses. They should not contain direct business logic or database queries.
