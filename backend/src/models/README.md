# Models

Models contain all raw database interactions (using PostgreSQL client queries). They represent schema structures and define methods to insert, query, update, and delete entities like Users, Projects, and Analysis Runs.
